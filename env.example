# Base URL used by user-groups.js and traffic-distribution.js
# Defaults to https://test.k6.io if not set.
# Not used by first-k6script.js, which targets SauceDemo directly (hardcoded).
#
# Usage:
#   k6 run -e BASE_URL=https://staging.example.com script/user-groups.js
BASE_URL=https://test.k6.io
